##--------------------------------------------------------------
/*
uint
bsearch(SV *haystack, SV *needle, uint nbits, ...)
PREINIT:
  const uchar *vhaystack, *vneedle;
  uint haystack_len, needle_len, ilo, ihi;
CODE:
 vhaystack = SvPV(haystack,haystack_len);
 vneedle = SvPV(needle,needle_len);
 if (items > 3) ilo = SvUV(ST(3));
 else ilo = 0;
 if (items > 4) ihi = SvUV(ST(4));
 else ihi = vlen * 8/nbits;
OUTPUT:
 RETVAL
*/
